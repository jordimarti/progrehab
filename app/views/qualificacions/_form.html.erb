<%= simple_form_for(@qualificacio) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :edifici_id %>
    <%= f.input :xml %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
