<%= simple_form_for([@edifici, @pagament]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :edifici_id, :as => :hidden, :input_html => { :value => @edifici.id } %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
